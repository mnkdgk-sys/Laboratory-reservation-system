# 実験室予約システム — セットアップ手順

研究室のPCに `実験室管理` フォルダをコピーした後、以下の手順を実行してください。

---

## 準備するもの

- 研究室用のメールアドレス（Supabase・Netlify のアカウント作成に使用）
- ターミナル（Mac）またはコマンドプロンプト（Windows）

---

## STEP 1 — Supabase プロジェクトを作成する

1. ブラウザで https://supabase.com を開く
2. 「Start your project」→ 研究室のアカウントでサインアップ
3. 「New Project」をクリックして新しいプロジェクトを作成
   - Project name: 任意（例: `lab-reservation`）
   - Database Password: 安全なパスワードを設定して **必ずメモしておく**
   - Region: Northeast Asia (Tokyo) を推奨

---

## STEP 2 — データベースをセットアップする

1. Supabase ダッシュボード左メニューの「**SQL Editor**」を開く
2. 「New query」をクリック
3. `schema.sql` ファイルの内容を**すべてコピー**して貼り付ける
4. 右上の「**Run**」ボタンをクリック
5. 「Success. No rows returned」と表示されれば完了

---

## STEP 3 — Auth（認証）を設定する

1. 左メニューの「**Authentication**」→「**Sign In / Providers**」
2. 「Email」をクリックして設定画面を開く
3. **「Confirm email」が表示されている場合は OFF にして Save**

---

## STEP 4 — 管理者アカウントを作成する

1. 「Authentication」→「**Users**」→「**Add user**」ボタンをクリック
2. 管理者のメールアドレスとパスワードを入力してアカウントを作成
3. 「**SQL Editor**」に戻り、以下のSQLを実行して管理者権限を付与:

```sql
UPDATE profiles SET role = 'admin' WHERE email = '管理者のメールアドレス';
```

4. また、メール確認が必要な場合は以下も実行:

```sql
UPDATE auth.users SET email_confirmed_at = NOW() WHERE email = '管理者のメールアドレス';
```

---

## STEP 5 — APIキーを取得して config.js を設定する

1. 左メニューの「**Project Settings**」→「**API**」を開く
2. 以下の2つをメモする:
   - **Project URL**: `https://xxxxxxxxxx.supabase.co`
   - **Publishable key** (または anon key): `sb_publishable_...` または `eyJ...`

3. `config.example.js` を **コピーして `config.js` という名前で保存**
4. `config.js` をテキストエディタで開き、実際の値を入力する:

```javascript
const CONFIG = {
  supabaseUrl: 'https://xxxxxxxxxx.supabase.co',  // ← Project URL
  supabaseKey: 'sb_publishable_...',               // ← Publishable key
};
```

> **注意**: `config.js` は `.gitignore` により GitHub にはアップロードされません。
> GitHub には `config.example.js`（値なし）のみ公開されます。

---

## STEP 6 — Netlify にアップロードする

1. ブラウザで https://app.netlify.com/drop を開く
2. 研究室アカウントでサインアップ/ログイン
3. `実験室管理` フォルダを画面にドラッグ&ドロップ
4. 発行された URL（例: `https://xxxxxxx.netlify.app`）をメモする

---

## STEP 7 — Supabase に Netlify の URL を登録する

1. Supabase ダッシュボード → 「Authentication」→「**URL Configuration**」
2. **Site URL** に STEP 6 で発行された URL を入力して Save

   例: `https://xxxxxxx.netlify.app`

---

## STEP 8 — Edge Function をデプロイする（招待・削除機能）

ターミナルを開いて以下を順番に実行してください:

```bash
# Supabase CLI のインストール（初回のみ）
brew install supabase/tap/supabase

# ログイン（ブラウザで認証）
supabase login

# 実験室管理フォルダに移動
cd ~/Desktop/実験室管理   # フォルダの場所に合わせて変更

# Supabase プロジェクトにリンク
# <project-ref> は Project URL の https:// の後、.supabase.co の前の文字列
supabase link --project-ref <project-ref>

# 招待機能をデプロイ
supabase functions deploy invite-user

# 削除機能をデプロイ
supabase functions deploy delete-user
```

---

## STEP 9 — 動作確認

1. ブラウザで STEP 6 の URL を開く
2. STEP 4 で作成した管理者アカウントでログイン
3. ログインできれば設定完了

---

## 日常の使い方

| 操作 | 手順 |
|------|------|
| メンバーを追加 | 設定 → メンバー管理 → 招待メール送信 |
| メンバーを削除 | 設定 → メンバー管理 → ✕ ボタン |
| 部屋を追加 | 設定 → 部屋 → ＋ 部屋を追加 |
| パスワード変更 | ヘッダーの名前ボタン → アカウント設定 |

---

## アプリを更新する場合

ファイルを編集した後、Netlify の管理ページ（https://app.netlify.com）を開いて
`実験室管理` フォルダを再度ドラッグ&ドロップしてください。

---

## トラブルシューティング

**ログインできない**
→ STEP 4 の `email_confirmed_at` を更新するSQL を実行してください

**招待メールのリンクを踏むとエラーになる**
→ STEP 7 の Site URL が正しいか確認してください

**招待・削除機能が動かない**
→ STEP 8 の Edge Function デプロイが完了しているか確認してください
